{% extends "base.html" %}
{% block cuerpo %}
{% if mensaje %}
{{mensaje}}
{% endif %}
<div class="centrar">
  <div class="cuadroGrande">
  <div class="titulo">Estado de Pacientes en Área de Emergencia</div>
  <table class="tablaEstados">
    <tr>
      <th>Número</th>
      <th>Nombre</th>
      <th>Hora de Ingreso</th>
      <th>Acciones Rapidas</th>
      <th>Clasificaci&oacute;n</th>
      <th>Alarma</th>
    </tr>
  	{% for l in lista %}
      <tr>
        <td>{{l.id}}</td>
        <td><span class="nombreLista">{{l.paciente}}</span></td>
        <td>{{l.hora_ingreso}}</td>
        {% if l.triage != 0 %}
          <td><a href="/emergencia/{{l.id}}/t1">Re-evaluar</a></td>
        {% else %}
          <td>
            <a href="/emergencia/{{l.id}}/t1"><img src="/static/img/ESI2.png" /></a>
            <a href="/emergencia/{{l.id}}/t2"><img src="/static/img/ESI2.png" /></a>
            <a href="/emergencia/{{l.id}}/triage/calcular"><img src="/static/img/Triage.png" /></a>
          </td>
        {% endif %}
				{% if l.triage != 0 %}
					<td><img src="/static/img/nivel{{ l.triage }}.png" /></td>
				{% else %}
					<td><div class="mostrarNivel nivel{{ l.triage }}"></div></td>				
				{% endif %}
					<td><img src="/static/img/alarmaVerde.png" /></td>
      </tr>
    {% endfor %}
  </table>
  </div>
</div>
{% endblock %}
        
        
