{% extends "base.html" %}
{% block cuerpo %}
{% if mensaje %}
{{mensaje}}
{% endif %}
<div class="centrar">
  <div class="cuadroGrande">
  <div class="titulo">Estado de Pacientes en Área de Emergencia</div>
  <table class="tablaEstados">
    <tr>
      <th>N&uacute;mero</th>
      <th>Nombre</th>
      <th>Hora de Ingreso</th>
      <th>Acciones Rápidas</th>
      <th>Clasificación</th>
      <th>Alarma</th>
    </tr>
  	{% for l in lista %}
      <tr>
        <td>{{l.id}}</td>
				<td><span class="nombreLista"><a a href='/emergencia/atencion/{{ l.id }}'>{{l.paciente}}</a></span></td>
        <td>{{l.hora_ingreso}}</td>
        {% if l.triage != 0 %}
          <td><a href="/emergencia/{{l.id}}/t1"><img src="/static/img/triage.png" /></a></td>
        {% else %}
          <td>
            <a href="/emergencia/{{l.id}}/t1"><img src="/static/img/ESI1.png" title="Colocar en Nivel 1"/></a>
            <a href="/emergencia/{{l.id}}/t2"><img src="/static/img/ESI2.png" title="Colocar en Nivel 2"/></a>
            <a href="/emergencia/{{l.id}}/triage/calcular"><img src="/static/img/triage.png" title="Realizar Triage"/></a>
          </td>
        {% endif %}
					<td>{% if l.triage != 0 %}<img src="/static/img/nivel{{ l.triage }}.png" />{% endif %}</td>
					<td><img src="/static/img/alarmaVerde.png" /></td>
      </tr>	
    {% endfor %}
  </table>
  </div>
</div>
{% endblock %}
